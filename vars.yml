rsys_conf: "/etc/rsyslog.conf"
rsys_dir: "/var/log/hosts/"

elasticsearch_version: "1.1"
elastic_conf: "/etc/elasticsearch/elasticsearch.yml"
logstash_version: "1.4"

sincedb: "/var/lib/logstash/"

nginx_confd: "/etc/nginx/conf.d"

usname: "topscore"
htpass: "password"
domain: "logs"

kibana_url: "https://download.elasticsearch.org/kibana/kibana/kibana-{{ kibana_version }}.tar.gz"
kibana_version: "3.0.1"

forwarder_install_dir: "/opt/logstash-forwarder"
forwarder_user: "grin"
forwarder_receiver_host: "log1"
forwarder_receiver_port: 28903
